name: vibe_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  # Fixed indentation: these are now peers to 'flutter'
  go_router: ^17.1.0
  flutter_svg: ^2.0.7
  cupertino_icons: ^1.0.8
  glassmorphism: ^3.0.0 

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.6.0
  go_router_builder: ^4.0.1
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/